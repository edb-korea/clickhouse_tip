# ClickHouse 3노드 / 3샤드 순환(Circular) 복제 토폴로지 구성

3개 노드로 3개 샤드 + 순환(Circular) 복제 토폴로지를 구성하는 실제 설정 예시입니다. 각 노드가 서로 다른 두 샤드의 replica를 겸하는 구조입니다.

## 토폴로지 설계

```
        Shard 1 (Node1 + Node2)
       ┌──────────┐  ┌──────────┐
       │  Node1   │  │  Node2   │
       │ (rep A)  │  │ (rep B)  │
       └──────────┘  └──────────┘

        Shard 2 (Node2 + Node3)
       ┌──────────┐  ┌──────────┐
       │  Node2   │  │  Node3   │
       │ (rep A)  │  │ (rep B)  │
       └──────────┘  └──────────┘

        Shard 3 (Node3 + Node1)
       ┌──────────┐  ┌──────────┐
       │  Node3   │  │  Node1   │
       │ (rep A)  │  │ (rep B)  │
       └──────────┘  └──────────┘
```

| 노드 | 담당 샤드/역할 |
|---|---|
| Node1 (10.0.0.1) | Shard1의 replica A, Shard3의 replica B |
| Node2 (10.0.0.2) | Shard1의 replica B, Shard2의 replica A |
| Node3 (10.0.0.3) | Shard2의 replica B, Shard3의 replica A |

각 노드가 **서로 다른 두 샤드**를 담당하므로, 스키마 충돌을 피하기 위해 **샤드별로 별도 데이터베이스**를 둡니다.

---

## 1. ZooKeeper/Keeper 설정 (3노드에 공통 적용)

```xml
<!-- /etc/clickhouse-server/config.d/zookeeper.xml -->
<clickhouse>
    <zookeeper>
        <node>
            <host>10.0.0.1</host>
            <port>2181</port>
        </node>
        <node>
            <host>10.0.0.2</host>
            <port>2181</port>
        </node>
        <node>
            <host>10.0.0.3</host>
            <port>2181</port>
        </node>
    </zookeeper>
</clickhouse>
```

> 참고: 프로덕션이라면 Keeper는 ClickHouse 서버와 분리된 전용 호스트에 두는 게 정석이지만, 노드가 3대뿐인 상황이라면 각 노드에 ClickHouse Keeper를 함께 구동하는 경우가 많습니다. 이 경우 리소스 경쟁으로 replica가 read-only 전환되지 않도록 Keeper용 리소스(CPU/메모리)를 별도로 예약해두는 걸 권장합니다.

## 2. remote_servers 설정 (3노드에 동일하게 적용)

```xml
<!-- /etc/clickhouse-server/config.d/remote_servers.xml -->
<clickhouse>
    <remote_servers>
        <circular_cluster>
            <shard>
                <internal_replication>true</internal_replication>
                <replica>
                    <host>10.0.0.1</host>
                    <port>9000</port>
                </replica>
                <replica>
                    <host>10.0.0.2</host>
                    <port>9000</port>
                </replica>
            </shard>
            <shard>
                <internal_replication>true</internal_replication>
                <replica>
                    <host>10.0.0.2</host>
                    <port>9000</port>
                </replica>
                <replica>
                    <host>10.0.0.3</host>
                    <port>9000</port>
                </replica>
            </shard>
            <shard>
                <internal_replication>true</internal_replication>
                <replica>
                    <host>10.0.0.3</host>
                    <port>9000</port>
                </replica>
                <replica>
                    <host>10.0.0.1</host>
                    <port>9000</port>
                </replica>
            </shard>
        </circular_cluster>
    </remote_servers>
</clickhouse>
```

`internal_replication=true`는 필수입니다 — Distributed 테이블이 INSERT 시 같은 샤드의 모든 replica에 중복으로 쓰지 않고, ReplicatedMergeTree 자체의 복제 메커니즘에 맡기도록 하는 설정입니다.

## 3. Node1 (10.0.0.1) DDL

```sql
-- Shard1의 replica A 역할
CREATE DATABASE IF NOT EXISTS shard1;

CREATE TABLE shard1.events
(
    event_id   UInt64,
    user_id    UInt64,
    event_type String,
    event_time DateTime
)
ENGINE = ReplicatedMergeTree('/clickhouse/tables/shard1/events', 'node1')
PARTITION BY toYYYYMM(event_time)
ORDER BY (user_id, event_time);

-- Shard3의 replica B 역할
CREATE DATABASE IF NOT EXISTS shard3;

CREATE TABLE shard3.events
(
    event_id   UInt64,
    user_id    UInt64,
    event_type String,
    event_time DateTime
)
ENGINE = ReplicatedMergeTree('/clickhouse/tables/shard3/events', 'node1_b')
PARTITION BY toYYYYMM(event_time)
ORDER BY (user_id, event_time);

-- 분산 조회용 Distributed 테이블 (전체 샤드 통합 뷰)
CREATE TABLE default.events_distributed AS shard1.events
ENGINE = Distributed(circular_cluster, '', events, rand());
```

## 4. Node2 (10.0.0.2) DDL

```sql
-- Shard1의 replica B 역할
CREATE DATABASE IF NOT EXISTS shard1;

CREATE TABLE shard1.events
(
    event_id   UInt64,
    user_id    UInt64,
    event_type String,
    event_time DateTime
)
ENGINE = ReplicatedMergeTree('/clickhouse/tables/shard1/events', 'node2')
PARTITION BY toYYYYMM(event_time)
ORDER BY (user_id, event_time);

-- Shard2의 replica A 역할
CREATE DATABASE IF NOT EXISTS shard2;

CREATE TABLE shard2.events
(
    event_id   UInt64,
    user_id    UInt64,
    event_type String,
    event_time DateTime
)
ENGINE = ReplicatedMergeTree('/clickhouse/tables/shard2/events', 'node2_b')
PARTITION BY toYYYYMM(event_time)
ORDER BY (user_id, event_time);

CREATE TABLE default.events_distributed AS shard1.events
ENGINE = Distributed(circular_cluster, '', events, rand());
```

## 5. Node3 (10.0.0.3) DDL

```sql
-- Shard2의 replica B 역할
CREATE DATABASE IF NOT EXISTS shard2;

CREATE TABLE shard2.events
(
    event_id   UInt64,
    user_id    UInt64,
    event_type String,
    event_time DateTime
)
ENGINE = ReplicatedMergeTree('/clickhouse/tables/shard2/events', 'node3')
PARTITION BY toYYYYMM(event_time)
ORDER BY (user_id, event_time);

-- Shard3의 replica A 역할
CREATE DATABASE IF NOT EXISTS shard3;

CREATE TABLE shard3.events
(
    event_id   UInt64,
    user_id    UInt64,
    event_type String,
    event_time DateTime
)
ENGINE = ReplicatedMergeTree('/clickhouse/tables/shard3/events', 'node3_b')
PARTITION BY toYYYYMM(event_time)
ORDER BY (user_id, event_time);

CREATE TABLE default.events_distributed AS shard2.events
ENGINE = Distributed(circular_cluster, '', events, rand());
```

## 6. 핵심 설정 포인트

### ZooKeeper 경로 규칙

- **`/clickhouse/tables/shard1/events`** — 샤드 내에서는 **동일한 ZK 경로**를 써야 replica로 인식됩니다 (Node1의 `shard1.events`와 Node2의 `shard1.events`가 같은 경로를 공유).
- **replica 이름은 노드마다 달라야** 합니다 (`'node1'`, `'node2'` 등) — 같으면 오류가 납니다.

### load_balancing 설정 (필수 권장)

순환 토폴로지에서는 반드시 아래 설정을 추가해야 합니다:

```xml
<!-- /etc/clickhouse-server/users.d/load_balancing.xml -->
<clickhouse>
    <profiles>
        <default>
            <load_balancing>in_order</load_balancing>
        </default>
    </profiles>
</clickhouse>
```

`load_balancing`을 `in_order`로 설정하지 않으면, ClickHouse가 쿼리 시 **같은 물리 노드에 있는 서로 다른 샤드의 replica 2개를 동시에 선택**해버릴 수 있습니다 — 이렇게 되면 사실상 한 노드가 두 샤드 몫의 쿼리를 동시에 처리하게 되어 병렬성 이점이 사라지고 그 노드에만 부하가 몰립니다.

## 7. 동작 확인

```sql
-- 각 노드에서 replica 상태 확인
SELECT database, table, replica_name, is_leader, is_readonly
FROM system.replicas;

-- 클러스터 전체 토폴로지 확인
SELECT cluster, shard_num, replica_num, host_name, host_address, port
FROM system.clusters
WHERE cluster = 'circular_cluster';

-- 분산 테이블로 전체 데이터 조회 테스트
INSERT INTO default.events_distributed VALUES (1, 100, 'click', now());
SELECT * FROM default.events_distributed;
```

## 참고 — 여전히 권장하지 않는 이유 재확인

이 구성은 **노드 3대만 있는 제약 상황에서 replica 이점을 최대한 살리려는 타협안**입니다. 노드 하나가 죽으면 **그 노드가 담당하던 두 샤드 각각이 replica 1개만 남는 상태**가 되므로, 별도 replica 노드를 쓰는 표준 구성보다 장애 내구성이 떨어집니다. 가능하다면 6노드(3샤드×2replica, 노드당 역할 1개)로 확장하는 것을 장기적으로 고려하시는 걸 추천드립니다.
