# ClickHouse 노드 구성 방법 — 토폴로지 방안 비교

ClickHouse 클러스터는 **샤드(Shard, 데이터 분산)**와 **레플리카(Replica, 데이터 복제)**를 조합하는 방식에 따라 여러 구성 방안이 있습니다. 각 방안의 구조, 장단점, 적합한 상황을 정리했습니다.

---

## 방안 1. 단일 노드 (No Sharding, No Replication)

```
┌─────────────────────┐
│       Node1          │
│  ┌────────────────┐  │
│  │  MergeTree      │  │
│  │  (전체 데이터)   │  │
│  └────────────────┘  │
└─────────────────────┘
```

| 항목 | 내용 |
|---|---|
| 필요 노드 수 | 1대 |
| 장점 | 설정 간단, 운영 부담 최소, Keeper 불필요 |
| 단점 | 장애 시 서비스 중단, 데이터 유실 위험, 확장 불가 |
| 적합한 상황 | 개발/테스트 환경, PoC, 소규모 단발성 분석 |

```sql
CREATE TABLE events (
    event_id UInt64,
    user_id UInt64,
    event_time DateTime
) ENGINE = MergeTree()
ORDER BY (user_id, event_time);
```

---

## 방안 2. 단일 샤드 + 다중 레플리카 (HA 전용, 확장성 없음)

```
        Shard 1 (전체 데이터 동일하게 복제)
   ┌──────────┐   ┌──────────┐   ┌──────────┐
   │  Node1   │   │  Node2   │   │  Node3   │
   │ Replica1 │   │ Replica2 │   │ Replica3 │
   └──────────┘   └──────────┘   └──────────┘
        │              │              │
        └──────────────┴──────────────┘
              ClickHouse Keeper
           (3-node quorum, 별도 또는 겸용)
```

| 항목 | 내용 |
|---|---|
| 필요 노드 수 | 2대 이상 (보통 3대: 짝수 회피, Keeper 쿼럼 고려) |
| 장점 | 노드 장애에도 서비스 지속, 읽기 쿼리 분산 가능 |
| 단점 | 데이터가 전체 노드에 동일하게 복제되므로 **저장 용량 확장 안 됨**, 쓰기는 여전히 병목 가능 |
| 적합한 상황 | 데이터 크기는 크지 않지만 고가용성이 중요한 서비스 |

```xml
<remote_servers>
    <ha_cluster>
        <shard>
            <internal_replication>true</internal_replication>
            <replica><host>node1</host><port>9000</port></replica>
            <replica><host>node2</host><port>9000</port></replica>
            <replica><host>node3</host><port>9000</port></replica>
        </shard>
    </ha_cluster>
</remote_servers>
```

---

## 방안 3. 다중 샤드 + 단일 레플리카 (확장 전용, HA 없음)

```
   Shard1        Shard2        Shard3
 ┌────────┐   ┌────────┐    ┌────────┐
 │ Node1  │   │ Node2  │    │ Node3  │
 │(1/3 데이터)│ │(1/3 데이터)│  │(1/3 데이터)│
 └────────┘   └────────┘    └────────┘
```

| 항목 | 내용 |
|---|---|
| 필요 노드 수 | 샤드 수만큼 (예: 3대) |
| 장점 | 데이터/쿼리 부하 분산으로 확장성 확보, 노드당 비용 최소 |
| 단점 | **레플리카가 없어 노드 하나만 죽어도 해당 샤드 데이터 접근 불가 (사실상 서비스 중단)** |
| 적합한 상황 | 유실되어도 재적재 가능한 데이터(로그 등), 비용에 매우 민감하고 HA가 필수는 아닌 경우 |

```xml
<remote_servers>
    <scale_only_cluster>
        <shard><replica><host>node1</host><port>9000</port></replica></shard>
        <shard><replica><host>node2</host><port>9000</port></replica></shard>
        <shard><replica><host>node3</host><port>9000</port></replica></shard>
    </scale_only_cluster>
</remote_servers>
```

⚠️ 실무에서는 이 구성 단독으로는 잘 쓰지 않고, 보통 방안 4(샤드+레플리카)로 바로 넘어갑니다.

---

## 방안 4. 다중 샤드 + 다중 레플리카 (표준 프로덕션 구성) ⭐ 권장

```
        Shard 1                Shard 2
   ┌──────────┐┌──────────┐┌──────────┐┌──────────┐
   │  Node1   ││  Node2   ││  Node3   ││  Node4   │
   │ Replica A││ Replica B││ Replica A││ Replica B│
   └──────────┘└──────────┘└──────────┘└──────────┘
        (1/2 데이터, 2중 복제)  (1/2 데이터, 2중 복제)

              +  별도 3-node Keeper 클러스터 (권장)
```

| 항목 | 내용 |
|---|---|
| 필요 노드 수 | 샤드 수 × 레플리카 수 (예: 2샤드×2레플리카 = 4대) + Keeper 3대(권장, 별도) |
| 장점 | 확장성 + 고가용성 모두 확보. 업계 표준 |
| 단점 | 노드 수가 가장 많이 필요 (비용 ↑) |
| 적합한 상황 | 프로덕션 환경, 데이터 크기와 가용성 요구가 모두 있는 경우 |

```xml
<remote_servers>
    <prod_cluster>
        <shard>
            <internal_replication>true</internal_replication>
            <replica><host>node1</host><port>9000</port></replica>
            <replica><host>node2</host><port>9000</port></replica>
        </shard>
        <shard>
            <internal_replication>true</internal_replication>
            <replica><host>node3</host><port>9000</port></replica>
            <replica><host>node4</host><port>9000</port></replica>
        </shard>
    </prod_cluster>
</remote_servers>
```

```sql
-- Node1 (Shard1 Replica A)
CREATE TABLE events ON CLUSTER prod_cluster (
    event_id UInt64, user_id UInt64, event_time DateTime
) ENGINE = ReplicatedMergeTree('/clickhouse/tables/{shard}/events', '{replica}')
ORDER BY (user_id, event_time);

CREATE TABLE events_distributed ON CLUSTER prod_cluster AS events
ENGINE = Distributed(prod_cluster, currentDatabase(), events, rand());
```
(`{shard}`, `{replica}` 매크로를 각 노드의 `macros.xml`에 정의해두면 `ON CLUSTER`로 전체 노드에 동일 DDL을 한 번에 적용 가능)

---

## 방안 5. 순환(Circular) 토폴로지 — 노드 부족 시 절충안

```
   Shard1(N1+N2)  Shard2(N2+N3)  Shard3(N3+N1)
   각 노드가 서로 다른 두 샤드의 replica를 겸함

        ┌────────┐   ┌────────┐   ┌────────┐
        │ Node1  │   │ Node2  │   │ Node3  │
        │S1-A,S3-B│  │S1-B,S2-A│  │S2-B,S3-A│
        └────────┘   └────────┘   └────────┘
```

| 항목 | 내용 |
|---|---|
| 필요 노드 수 | 샤드 수와 동일 (예: 3대로 3샤드×2레플리카 효과) |
| 장점 | 방안4 대비 **노드 수를 절반으로 절약**하면서도 어느 정도 HA 확보 |
| 단점 | 설정 복잡, `load_balancing=in_order` 필수, 노드 1대 장애 시 두 샤드가 동시에 레플리카 1개로 축소, INSERT 부하 2배 |
| 적합한 상황 | 노드 수가 제한적이지만 최소한의 HA는 필요한 경우 (앞서 상세 예제로 다룸) |

> 자세한 설정 파일 예시는 별도 문서(`clickhouse_circular_topology_3node_3shard.md`) 참고

---

## 방안 6. Keeper 분리형 구성 (프로덕션 권장 패턴)

```
   ClickHouse 데이터 노드              전용 Keeper 클러스터
 ┌────────┐┌────────┐┌────────┐      ┌────────┐┌────────┐┌────────┐
 │ CH-1   ││ CH-2   ││ CH-3   │      │Keeper-1││Keeper-2││Keeper-3│
 │(샤드/레플리카)│      (경량, 4GB RAM 내외)
 └────────┘└────────┘└────────┘      └────────┘└────────┘└────────┘
        └───────┴───────┘                  │
              조정(coordination) 트래픽 ─────┘
```

| 항목 | 내용 |
|---|---|
| 필요 노드 수 | ClickHouse 데이터 노드 N대 + Keeper 전용 3대 |
| 장점 | Keeper가 리소스 경쟁 없이 안정적으로 동작 → replica read-only 전환 위험 최소화 |
| 단점 | 노드 수가 가장 많이 필요 |
| 적합한 상황 | 미션 크리티컬 프로덕션, 대규모 클러스터 |

```xml
<!-- Keeper 전용 노드 (Keeper-1) config -->
<clickhouse>
    <keeper_server>
        <tcp_port>9181</tcp_port>
        <server_id>1</server_id>
        <coordination_settings>
            <operation_timeout_ms>10000</operation_timeout_ms>
            <session_timeout_ms>30000</session_timeout_ms>
        </coordination_settings>
        <raft_configuration>
            <server><id>1</id><hostname>keeper-1</hostname><port>9234</port></server>
            <server><id>2</id><hostname>keeper-2</hostname><port>9234</port></server>
            <server><id>3</id><hostname>keeper-3</hostname><port>9234</port></server>
        </raft_configuration>
    </keeper_server>
</clickhouse>
```

---

## 방안 7. 클라우드 네이티브형 — Storage/Compute 분리 (SharedMergeTree 계열)

```
   컴퓨트 노드(무상태, 자유롭게 증감)
 ┌────────┐┌────────┐┌────────┐
 │ CH-1   ││ CH-2   ││ CH-3   │
 └───┬────┘└───┬────┘└───┬────┘
     └─────────┼─────────┘
               │
       ┌───────▼────────┐
       │ 공유 오브젝트 스토리지 │   (S3 / Azure Blob 등)
       │  (실제 데이터 저장)   │
       └────────────────┘
```

| 항목 | 내용 |
|---|---|
| 필요 노드 수 | 컴퓨트 노드는 필요시 자유롭게 증감 (스토리지와 분리) |
| 장점 | ClickHouse Cloud처럼 컴퓨트/스토리지 독립적 확장, 로컬 replica 개념 자체가 단순화됨 |
| 단점 | 온프레미스 자체 구축은 복잡 (보통 ClickHouse Cloud 등 관리형 서비스로 이용) |
| 적합한 상황 | 클라우드 관리형 서비스 사용 시, 트래픽 변동이 크고 탄력적 확장이 중요한 경우 |

---

## 전체 비교 요약표

| 방안 | 필요 노드 수(예) | 확장성 | 고가용성 | 복잡도 | 실무 권장도 |
|---|---|---|---|---|---|
| 1. 단일 노드 | 1 | ❌ | ❌ | 최소 | 개발/테스트만 |
| 2. 단일 샤드+다중 레플리카 | 3 | ❌ | ✅ | 낮음 | 소규모 HA 서비스 |
| 3. 다중 샤드+단일 레플리카 | 3 | ✅ | ❌ | 낮음 | 비권장(단독 사용 지양) |
| 4. 다중 샤드+다중 레플리카 | 4~ | ✅ | ✅ | 중간 | ⭐ 표준 프로덕션 |
| 5. 순환 토폴로지 | 3 (절약형) | ✅(제한적) | △ | 높음 | 노드 부족 시 절충안 |
| 6. Keeper 분리형 | N+3 | ✅ | ✅✅ | 중간~높음 | 대규모 프로덕션 |
| 7. 클라우드 네이티브 | 가변 | ✅✅ | ✅✅ | 관리형 서비스 의존 | 클라우드 사용 시 |

## 선택 가이드

```
데이터가 작고 개발/테스트 목적인가?          → 방안 1
데이터는 작지만 무중단이 중요한가?            → 방안 2
데이터가 크고 비용이 최우선, 유실 허용 가능?   → 방안 3 (권장하진 않음)
데이터도 크고 무중단도 필요한 표준 프로덕션?   → 방안 4 (기본 선택지)
노드가 부족한데 최소한의 HA는 필요한가?        → 방안 5
대규모 클러스터로 안정성을 극대화하고 싶은가?   → 방안 6
클라우드에서 탄력적 확장을 원하는가?           → 방안 7 (ClickHouse Cloud 등)
```

가장 흔한 실무 선택은 **방안 4(다중 샤드+다중 레플리카) + 방안 6(Keeper 분리)**를 함께 적용하는 것이며, 노드 수가 정말 제한적인 초기/PoC 단계에서만 방안 5(순환 토폴로지)를 임시로 고려하는 흐름이 일반적입니다.
